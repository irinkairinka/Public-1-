
per_cent = {'ТКБ': 5.6, 'СКБ': 5.9, 'ВТБ': 4.28, 'СБЕР': 4.0}

m = input("Какую сумму Вы планируете положить в банк?")
res = [int(float(m) * per_cent['ТКБ'] / 100), int(float(m) * per_cent['СКБ'] / 100),int(float(m) * per_cent['ВТБ'] / 100),int(float(m) * per_cent['СБЕР'] / 100),]

print("При депозите на сумму", int(m) , "рублей Вы получите доход:")
print("-в банке ТКБ в размере", res[0], "рублей,")
print("-в банке СКБ в размере", res[1], "рублей,")
print("-в банке ВТБ в размере", res[2], "рублей,")
print("-в СБЕРБАНКе в размере", res[3], "рублей.")

print("Максимальная сумма, которую вы можете заработать, составит", max(res), "рублей.")
